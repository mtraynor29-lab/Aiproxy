{
  "name": "hcnsec-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker reverse proxy for the HCNSEC OpenAI-compatible AI API",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.34.0"
  }
}
